# Page and section architecture

Each route family has its own page folder and a nested `sections` folder. Shared site routing lives in `components/site`, while reusable page sections are grouped by customer account, quotation, public commerce, public content, and administration. Styling is provided by Tailwind CSS v4 in `app/globals.css`.
